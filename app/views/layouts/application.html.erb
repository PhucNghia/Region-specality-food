<!DOCTYPE html>
<html>
  <head>
    <title>Đặc sản 3 miền</title>
    <%= include_gon %>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbolinks-track": "reload" %>
    <%= favicon_link_tag "favicon.jpg" %>
  </head>

  <body>
    <div class="site">
      <%= render "layouts/header" %>
      <div id="alert-notification" class="alert-notification alert-notification-dismissible">
        <a class="close" data-dismiss="alert-notification" aria-label="close">&times;</a>
      </div>
      <% if flash.present? %>
        <%= render "shared/flash_messages" %>
      <% end %>
      <% if action_name == "home" %>
        <div style="padding-top: 0">
      <% else %>
        <div style="padding-top: 100px;">
      <% end %>
        <%= yield %>
      </div>
      <%= render "layouts/footer" %>
    </div>

    <input class="statistical" value="<%= session[:statistical] %>" type="text">
  </body>
</html>
